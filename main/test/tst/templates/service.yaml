apiVersion: v1
kind: Service
metadata:
  name: {{ include "tst.fullname" . }}
  labels:
    {{- include "tst.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tst.selectorLabels" . | nindent 4 }}
