use-cases {
    upload dsl
    download dsl
    list dsl

    activate rule
    deactivate rule
    list rules
}

GET, POST, PUT, DELETE /dsl
TYPE file: upload source code

GET /servers -> list running servers
GET /servers/{i} -> inspect server
POST /servers
json body {
    dslId
}
DELETE /servers/{i}

